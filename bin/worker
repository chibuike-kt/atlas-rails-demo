#!/usr/bin/env php
<?php
declare(strict_types=1);

require __DIR__ . '/../vendor/autoload.php';

use App\Queue\Worker;

echo "Worker started. FAILPOINTS=" . (getenv('FAILPOINTS') ?: '') . "\n";
(new Worker())->runForever(250);
